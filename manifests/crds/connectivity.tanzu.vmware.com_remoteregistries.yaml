
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: remoteregistries.connectivity.tanzu.vmware.com
spec:
  group: connectivity.tanzu.vmware.com
  names:
    kind: RemoteRegistry
    listKind: RemoteRegistryList
    plural: remoteregistries
    singular: remoteregistry
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RemoteRegistry ...
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RemoteRegistrySpec...
            properties:
              address:
                type: string
              allowedDomains:
                items:
                  type: string
                type: array
              tlsConfig:
                description: RegistryTLSConfig...
                properties:
                  serverCA:
                    format: byte
                    type: string
                  serverName:
                    type: string
                required:
                - serverCA
                - serverName
                type: object
            required:
            - address
            - tlsConfig
            type: object
          status:
            description: RemoteRegistryStatus...
            properties:
              conditions:
                description: List of status conditions to indicate the status of RemoteRegistry.
                items:
                  description: RemoteRegistryCondition defines an observation of a RemoteRegistry resource state
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime describes the last time the condition transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: Message is a human readable description of the details of the last transition, complementing reason.
                      type: string
                    reason:
                      description: Reason is a brief machine readable explanation for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of ('True', 'False', 'Unknown').
                      type: string
                    type:
                      description: Type of the condition, known values are ('Valid').
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the latest generation observed by the controller.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
